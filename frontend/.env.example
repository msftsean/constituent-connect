# The frontend uses relative /api and /health calls. Do not set these to
# localhost URLs in Codespaces; the browser cannot reach the backend container
# through your local machine's 127.0.0.1. Use the forwarded 8000 primary app
# route, or the Vite 5173 route with the checked-in proxy.
